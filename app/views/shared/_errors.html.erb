<%# this is a form partial%>

<%# In order to display the messages, we add the 
    following code block to the edit.html.erb template.
    This is the same template as new.html%>
<%# if @article.errors.any? %> <%# <- old code that refers to articles object%>
<%# if obj.errors.any? %> <%#name gives name of class %>

  <% if @jab.errors.any? %>
  <h2>Your lack of practice prevented a jab from being thrown</h2>
    <ul>
        <% @jab.errors.full_messages.each do |msg| %>
        <li><%= msg %> </li>
        <% end %>
    </ul>
    <% end %>