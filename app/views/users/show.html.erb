<h1> <%= @user.username %>'s profile </h1>

<%= gravatar_for @user, size: 200 %>

    <div>
    <% if logged_in? && @user == current_user %>
        <%= link_to "Edit your profile", edit_user_path(@user) %>
    </div>

<br>

</br>

<h3> Jabs </h3>

<div> <%= will_paginate @jabs, :container => false %>
</div>

<%= render 'jabs/jab' %>

<% end %>